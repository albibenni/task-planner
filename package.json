{
  "name": "task-planner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "task-planner": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "check": "pnpm run typecheck && pnpm run lint && pnpm test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "test": "vitest run",
    "test:integration": "bash -c 'set -e; trap \"docker compose -f compose.integration.yaml down --volumes\" EXIT; docker compose -f compose.integration.yaml up --wait; TEST_DATABASE_URL=postgres://task_planner_test:task_planner_test@127.0.0.1:54329/task_planner_test pnpm exec vitest run src/database.integration.test.ts'",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.build.json --outDir dist",
    "auth": "tsx src/cli.ts auth",
    "run": "tsx src/index.ts run",
    "dry-run": "tsx src/index.ts run --dry-run",
    "prepare": "husky"
  },
  "dependencies": {
    "postgres": "^3.4.9",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@types/node": "^24.0.0",
    "husky": "^9.1.7",
    "tsx": "^4.20.0",
    "typescript": "~7.0.2",
    "vitest": "^4.1.10"
  }
}
